dist/
*.log
.DS_Store
# Go build artifacts — anchored to the repo root on purpose. Unanchored, these
# patterns also match the cmd/doc-agent-ai/ directory, which silently ignores
# any new source file added there.
/doc-agent-ai
/doc-agent-ai.exe
dist-release/
docs/*
!docs/assets/
docs/assets/*
!docs/assets/brand/
docs/assets/brand/*
!docs/assets/brand/Zeen_Dark.png
# Local IDE / tooling caches
.windsurf/
.atl/
.engram/
# CodeGraph index — per-checkout local state, never shared between worktrees
.codegraph/

# Local Pi runtime state
.pi/
.pi-lens/
agent-skill-persona-filosophy.md

# Go test coverage artifacts
coverage
coverage.out
